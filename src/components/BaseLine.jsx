import React from 'react'

const BaseLine = ({className=""}) => {
  return (
    <div className={`bg-gradient-to-r bg-primary-end to-primary-start mt-2 h-1 w-32 ${className}`}>
      
    </div>
  )
}

export default BaseLine
